public class Second {
    public static void main(String[] args) {
        double a = 1, b = 1, c = 1;
        double x = ((b + Math.sqrt(Math.pow(b, 2) + 4 * a * c)) / 2 * a) - Math.pow(a, 2) *c + Math.pow(b, 2)-2;
    }
}
